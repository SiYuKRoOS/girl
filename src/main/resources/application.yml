
spring:
  profiles:
    active: dev
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dbgirl
    username: root
    password: wzqqtozsy
  jpa:
    hibernate:
    #create 表示每次程序运行时都会创建一个空的表
      ddl-auto: create
    show-sql: true
